{% extends 'base.html.twig' %}

{% block body %}
    {% if is_granted('ROLE_USER') %}
        {{ include('index/logged-in.html.twig') }}
    {% else %}
        {{ include('index/anonymous.html.twig') }}
    {% endif %}
{% endblock %}
